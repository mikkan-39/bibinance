import { createSlice } from "@reduxjs/toolkit";

const initialState = {};

export const tradingPageSlice = createSlice({
  name: "tradingPage",
  initialState,
  reducers: {},
});

export const {} = tradingPageSlice.actions;

export default tradingPageSlice.reducer;
