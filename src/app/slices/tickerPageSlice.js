import { createSlice } from "@reduxjs/toolkit";

const initialState = {};

export const tickerPageSlice = createSlice({
  name: "tickerPage",
  initialState,
  reducers: {},
});

export const {} = tickerPageSlice.actions;

export default tickerPageSlice.reducer;
